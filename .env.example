# Export this in your shell; never commit the real value.
TRANSIT_LLM_API_KEY=

# Optional. The app discovers the GPT-5.5 model ID when omitted.
TRANSIT_LLM_MODEL=

# Optional; anonymous MBTA requests work at a lower rate limit.
MBTA_API_KEY=
